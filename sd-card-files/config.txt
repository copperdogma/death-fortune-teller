# Death, the Fortune-Telling Skeleton -- Configuration
# Copy this file to your SD card as /config/config.txt

# Basic settings
speaker_name=JBL Flip 5
speaker_volume=100

# WiFi Settings for OTA update/monitoring
# Uncomment and fill in your WiFi credentials to enable wireless features
#wifi_ssid=YourNetworkName
#wifi_password=YourPassword
#ota_hostname=death-fortune-teller
#ota_password=your_secure_ota_password

# Jaw servo range (microseconds)
# Override if your servo has different limits (e.g., 900-2200 for tested servo)
servo_us_min=900
servo_us_max=2200

# Capacitive finger sensor tuning
finger_cycles_init=0xFFFF
finger_cycles_measure=0xFFFF
finger_filter_alpha=0.1
finger_baseline_drift=0.0001
finger_multisample=32
finger_detect_ms=120
cap_threshold=0.10   # Sensitivity margin (0.0-1.0); higher = less sensitive (adaptive threshold = noise * (1 + value))

# Timing
finger_wait_ms=6000
snap_delay_min_ms=1000
snap_delay_max_ms=3000
cooldown_ms=12000

# Printer
printer_baud=9600
printer_logo=/printer/logo_384w.bmp
fortunes_json=/printer/fortunes_littlekid.json
